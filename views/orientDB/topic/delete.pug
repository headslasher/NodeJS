extends ../layout
block content
    article
      h1= 'Delete this article?'+topic.title
      - rid = encodeURIComponent(topic['@rid'])
      form(action='/topic/'+rid+'/delete' method='post')
        p
          input(type='submit' value='DELETE')
      a(href='/topic/'+rid) List
